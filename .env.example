# Optional shell environment. The app stores normal configuration in
# ~/.local-agent-gateway/config.json through the Web UI.

# Override the Codex home directory if you do not use ~/.codex.
# CODEX_HOME=/Users/you/.codex

